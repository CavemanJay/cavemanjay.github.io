procs:
  trunk: "trunk serve -i node_modules"
  bacon clippy: "bacon -j clippy"
  bacon tests: "bacon -j test"
  # tailwind: "npx tailwindcss -i ./input.css -o ./style/site.css --watch"
  tailwind: "pnpm run tailwind-watch"